    ```
    brew install ruby
    brew install node
    gem install bundler
    ```
1. `bundle install`


2. Run `jekyll serve -l -H localhost` to generate the HTML and serve it from `localhost:4000` 

## Using Docker

```bash
chmod -R 777 .
docker compose up
```